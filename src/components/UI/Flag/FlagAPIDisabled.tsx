import { type FC } from 'react';

import Flag from './Flag';

// component
const FlagAPIDisabled: FC = () => {
  return <Flag text='API Disabled' color='white' bgcolor='secondary.dark' />;
};

export default FlagAPIDisabled;
